1. Информативные комментарии

    // функция заполняет поле ячейками
    def fill_cells(N, M, field, cell):

    // формируем map id->salary
    map_ids_salary = dict(zip(sorted(ids), sorted(salary)))

2. Представление намерений

    // формируем матрицу переходов для удобной обработки
    transition = {
            (1,2): 1, (1,5): 2 ** 0.5, (1,6): 1, (1,8): 2 ** 0.5, (1,9): 1,
            
    // основной поток ожидает подключения
    sock = serv.accept();
    
3. Прояснение

    // tree_in -- структура дерева, которое нужно экспортировать
    def export_tree(tree_in):
    
    // Т -- число шагов, на которое нужно повернуть матрицу
    def MatrixTurn(Matrix, M, N, T):
    
4. Предупреждения о последствиях

    // высокое значение параметра может повесить систему
    Thread.setThreadCount(n)
    
    // метод может затереть ваши данные
    File.open(path)
    
5. Усиление

    // для правильной работы сортировка дб обратной
    sort_orders = sorted(orders.items(), key=lambda x: x[1], reverse=True)
    
    // редактор воспринимает только цифровые команды
    if N.isdigit():
    
6. Комментарии TODO

    // TODO: сделать рефакторинг, заменить массив на очередь
    
    // TODO: переименовать переменные модуля в соответствии с чистым кодом