7.1. именование булевых переменных

    flag - isSorted
    // флаг, который устанавливается, если значения списка отсортированы
    
    active - isActive
    // статус активности объекта
    
    parsed - parsingComplete
    // завершение парсинга
    
    connectionLost - connectionAlive
    // утвердительное имя статуса соединения
    
    filled - isBoardFilled
    // заполнено ли игровое поле

7.2. типичные имена булевых переменных

    found, success и done - имена, которые чаще всего можно использовать как типичные.

7.3. имена в циклах

    да, чаще всего в коде использую i j k имена.
    выразительне имена чаще всего стараемся использовать во вложенных циклах, например:
    for (row in rows):
        for (cell in row):
    
7.4. пары имён - антонимы

    в задаче про скобки я использовал open и close:
    if open == N and close == N:
    
    minValue и maxValue
    // минимальное и максимальное значение, найденные в массиве

7.5. Всем ли временным переменным в вашем коде присвоены выразительные имена? Найдите несколько случаев, когда временные переменные надо переименовать, и поищите, возможно, от некоторых временных переменных вам получится вообще полностью избавиться.

     нет, не всем. чаще всего это короткие имена, tmp имена или просто result.